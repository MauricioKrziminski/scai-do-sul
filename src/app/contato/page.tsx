import { Section } from "@/components/ui/section";

export const metadata = { title: "Contato" };

export default function Page() {
  return (
    <Section index="00" label="Em construção" title="Contato" intro="Esta página entra nas próximas etapas do projeto." />
  );
}
