import { Section } from "@/components/ui/section";

export const metadata = { title: "Produtos" };

export default function Page() {
  return (
    <Section index="00" label="Em construção" title="Produtos" intro="Esta página entra nas próximas etapas do projeto." />
  );
}
